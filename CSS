📋 1️⃣ Folder Structure for CSS
Place the CSS file in the static/css directory:

css
Copy
Edit
src/main/resources/static/css/style.css
📋 2️⃣ style.css (For Both Login & Registration Pages)
css
Copy
Edit
/* Global Styles */
body {
    background: linear-gradient(135deg, #2c3e50, #4ca1af);
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    margin: 0;
    padding: 0;
}

/* Card Container */
.card {
    background: #ffffff;
    padding: 30px;
    border-radius: 15px;
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
    width: 350px;
    transition: transform 0.3s ease-in-out;
}

.card:hover {
    transform: translateY(-5px);
}

/* Form Elements */
.form-label {
    font-weight: bold;
    color: #2c3e50;
}

.form-control,
.form-select {
    border-radius: 8px;
    border: 1px solid #ccc;
    padding: 10px;
    transition: border 0.3s;
}

.form-control:focus,
.form-select:focus {
    border-color: #4ca1af;
    box-shadow: none;
}

/* Buttons */
.btn-primary,
.btn-success {
    background-color: #4ca1af;
    border: none;
    padding: 10px;
    font-weight: bold;
    transition: background-color 0.3s ease;
    border-radius: 8px;
}

.btn-primary:hover,
.btn-success:hover {
    background-color: #2c3e50;
    color: #fff;
    transform: scale(1.05);
}

/* Links */
a {
    color: #4ca1af;
    text-decoration: none;
    transition: color 0.3s ease;
}

a:hover {
    color: #2c3e50;
    text-decoration: underline;
}

/* Responsive Design */
@media (max-width: 480px) {
    .card {
        width: 90%;
        padding: 20px;
    }
}
📋 3️⃣ Linking CSS in HTML Files
In both login.html and register.html, add this line in the <head> section:

html
Copy
Edit
<link rel="stylesheet" href="/css/style.css">
